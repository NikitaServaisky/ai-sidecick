import { StyleSheet } from "react-native"
export default StyleSheet.create({
    dayButton: {
        padding: 10,
        marginRight: 8,
        borderWidth: 1,
        borderColor: "#ccc",
        borderRadius: 10,
    },
    daySelected: {
        backgroundColor: "#def",
        borderColor: "#39f",
    }
})